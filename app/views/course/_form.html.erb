<%= simple_form_for @course, :url => course_index_path(:name => @course.name, :desc => @course.description) do |f| %>
  <%= f.input :name, required: true %>
  <%= f.input :description %>
  <%= f.button :submit %>

<% end %>